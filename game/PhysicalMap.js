// The map with all its objects

class PhysicalMap {
  constructor(width, height, objects, playerObject) {
    this.width = width;
    this.height = height;
    this.objects = objects;
    this.playerObject = playerObject;
  }
}